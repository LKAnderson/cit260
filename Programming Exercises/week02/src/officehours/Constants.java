package officehours;

public class Constants {

    // Don't have to do this for this week's assignments.
    final double CONVERSION_FACTOR = (9.0 / 5.0);
    
    
    public double celsiusToFahrenheit(double celsius) {
        return ((9.0/5.0) * celsius) + 32;
    }
    
}
